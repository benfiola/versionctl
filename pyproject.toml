[project]
name = "versionctl"
version = "0.0.0"
dependencies = [
    "click",
    "pydantic",
    "toml"
]

[project.optional-dependencies]
dev = [
    "nuitka"
]

[project.scripts]
versionctl = "versionctl.cli:main"

[tool.setuptools.package-data]
versionctl = ["data/**/*"]